[project]
name = "codex-real"
version = "0.1.1"
description = "Practical greeting CLI"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
codex-real = "src.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
